[project]
name = "runpod-nemotron-ocr"
version = "0.1.0"
description = "RunPod Serverless worker for NVIDIA Nemotron OCR v2."
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
